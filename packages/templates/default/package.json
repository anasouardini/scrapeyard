{
  "name": "projectname",
  "private": true,
  "description": "This project was made using a framework called Scrapeyard.",
  "main": "./src/index.ts",
  "license": "MIT",
  "keywords": [
    "scrapeyard"
  ],
  "repository": {
    "url": ""
  },
  "scripts": {
    "createProject": "./node_modules/.bin/createProject",
    "start": "ts-node -T ./src/index.ts",
    "test": "vitest",
    "testOnce": "vitest --run",
    "lint": "echo 'linting...'",
    "prebuild": "[ -d ./scripts ] || ln -s ../scripts scripts",
    "build": "pnpm run prebuild && echo 'building...' && pnpm run postbuild",
    "postbuild": "node ./scripts/cp-to-create-scrapeyard.js"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "scrapeyard": "link:../../scrapeyard"
  },
  "devDependencies": {
    "@types/node": "^20.4.5",
    "ts-node": "^10.9.1",
    "vitest": "^1.2.2"
  }
}
