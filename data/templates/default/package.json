{
  "name": "projectname",
  "version": "0.0.0",
  "description": "",
  "main": "./src/index.ts",
  "license": "MIT",
  "keywords": [
    "scrapeyard"
  ],
  "repository": {
    "url": "https://github.com/anasouardini/projectname"
  },
  "bugs": {
    "url": "https://github.com/anasouardini/projectname/issues"
  },
  "scripts": {
    "test": "vitest",
    "format": "prettier . --write",
    "testOnce": "vitest --run",
    "formatCheck": "prettier . --check",
    "lint": "tsc --noEmit",
    "build": "tsc",
    "ci": "npm run formatCheck && npm run lint && npm run testOnce && npm run build"
  },
  "dependencies": {}
}
